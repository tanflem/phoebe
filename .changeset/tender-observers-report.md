---
"phoebe-agent": minor
---

Add a versioned, read-only `phoebe status --json` observer contract with atomic
engine snapshots, bounded sanitized events, effective non-secret configuration,
engine provenance, and opt-in Docker discovery labels.
